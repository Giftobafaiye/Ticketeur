/** Build the import-env-vars body. Omit `isSecret` unless `--secret` so an existing secret is not demoted. */
export declare function buildEnvSetImportBody(name: string, value: string, secret: boolean): {
    variables: {
        [name]: string;
    };
    override: true;
    isSecret?: true | undefined;
};
